# 表格内容规范

先确定每行代表的对象与各列的含义、单位和口径，再组织数据。缺失值留空，不用零或推测值填补。来源与业务数据分开，保留行与来源的对应关系。表头、冻结行、筛选和列宽应支持实际阅读。

标识符和原始文字保留文本类型，避免丢失前导零或把输入误当公式。需要计算时明确数据类型、公式和单位，使用支持这些能力的工作簿接口；资料整理表不能冒充可计算模型。文件结构验证不证明公式计算正确，未经过计算引擎不得声称重算完成。
