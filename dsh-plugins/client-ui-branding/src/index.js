export const name = 'client-ui-branding'

// Presentation lives entirely in the Client half. Keeping the Host half
// empty makes removal and replacement a package-manifest operation.
export function apply() {}
