# ChatECNU Work Conversation copy derivative

[简体中文](README.md)

This product-owned package is rebuilt from the locked DSH Conversation plugin
without modifying the upstream checkout. It changes the empty-session hero
copy and its release-channel badge. Running-turn status now belongs to the
official `@deepseek-ai/dsh-client-ui-chat` implementation and is no longer
patched here. Artifact preview shadows the official `details` slot only while
a preview is open, so this derivative exposes just one narrow add-control child
seat at the resident “＋”. Without a registrant, the locked official command
button remains the fallback. See `UPSTREAM.md` for the replacement boundary
and retirement rule.
