# Upstream boundary

- Upstream package: `@deepseek-ai/dsh-client-ui-conversation@0.1.2-rc.1`
- Locked commit: `a66e4702047846cdaa10c66c9d3df3951f5ea70d`
- Product delta: package identity, empty-conversation copy, release-channel
  badge and one unified add-control child seat that receives the official
  command, image-intake and editor-reference callbacks.
- Build gate: every replacement anchor must occur exactly once.
- Retirement: remove this package and restore the official row when DSH exposes
  public hero-copy/badge configuration and a general add-control seam with the
  callbacks needed to merge commands, images and workspace files.

The official runtime package remains byte-for-byte identical to the locked DSH
npm release or reviewed checkout. This derivative is independently removable
and replaceable.
# DSH 0.1.5 compatibility

The source-development build also accepts DSH 0.1.5-alpha.1 after the shared
source contract gate. Its bundler places adjacent hero spans on the same line;
the product badge patch matches the unique previewBadge node instead of fixed
indentation. Native file intake remains owned by DSH. The checked-in legacy
artifact is retained for the pinned production profile; development assembly
rebuilds a version-checked derivative from its exact official Runtime.
