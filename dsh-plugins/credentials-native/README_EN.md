# Native credential service

[简体中文](README.md)

Exposes credential set, resolve, describe and unset operations through the private authenticated desktop bridge, backed by Windows Credential Manager.

Credentials are not written to `.credentials.yaml`.
